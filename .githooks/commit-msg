#!/bin/sh
# Reject commit messages containing "Claude-Session" or links to claude.ai / anthropic.com.
if grep -qiE "Claude-Session|claude|anthropic" "$1"; then
	echo "commit-msg: message contains 'Claude-Session' or a claude/anthropic link, remove it" >&2
	exit 1
fi
